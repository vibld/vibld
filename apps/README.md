# Applications

This directory will contain deployable Vibld applications. M1 is expected to introduce a web interface and a minimal API boundary only when the first prompt-to-preview slice requires them.
