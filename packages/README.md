# Packages

Reusable capabilities graduate here when implementation proves a stable boundary. Expected domains include agent orchestration, AI providers, core project models, Git, previews, sandboxes, templates, editor integration, UI, and shared utilities. The list is directional, not a requirement to create empty packages.
