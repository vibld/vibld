{
  "name": "@vibld/eval",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc --noEmit",
    "check": "pnpm run typecheck && pnpm run test",
    "eval": "node --experimental-strip-types bin/eval.ts",
    "lint": "tsc --noEmit",
    "test": "node --test --experimental-strip-types test/*.test.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@vibld/core": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "26.4.1",
    "typescript": "7.0.2"
  }
}
