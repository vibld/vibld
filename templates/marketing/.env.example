# Public site origin. Used to build absolute URLs for canonical links and
# social card metadata, which crawlers require to be absolute.
#
# Copy this file to .env and set it to wherever the site is actually served.
# The build falls back to a placeholder so it never fails on a fresh clone --
# but a placeholder in a social card is a broken social card.
VITE_SITE_URL=https://example.com
